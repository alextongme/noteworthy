# Noteworthy

## Render Deployment

Before calling any Render MCP tools (e.g., `list_services`, `list_deploys`), always call `list_workspaces` first to ensure a workspace is selected. The workspace ID is `tea-d6kus4fgi27c738ulh2g` and the noteworthy service ID is `srv-d6kv1f94tr6s73cl51c0`. Auto-deploy is enabled on the `master` branch, so pushing to master triggers a deploy.
